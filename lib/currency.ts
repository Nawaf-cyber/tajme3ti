/* ============ العملة ============
 *
 * كازاسوق متجرٌ متعدّد العملات، ويخدم واجهة البحرين لبعض العناوين — ومنها
 * خوادمُنا وخوادمُ الوسيط. فصفحته تُعلن «BHD 57.00» لقطعةٍ سعرها عندنا ٥٧٠
 * ريالاً.
 *
 * ⚠️ والقاعدة مكتوبةٌ في الساحب منذ زمن، وكانت على وشك أن تُكتب ثانيةً في
 * محرّك البحث. وتكرار منطقٍ كهذا ليس تكراراً تجميليّاً: نسختان تفترقان،
 * فيقرأ مسارٌ الدينارَ ريالاً بينما يُصحّحه الآخر — والنتيجة سعرٌ يبدو
 * معقولاً وهو عُشر الحقيقة. وهذا عطلٌ حقيقيٌّ وقع عندنا مع كازاسوق من قبل.
 *
 * فالمصدر واحدٌ هنا.
 */

/** الدينار البحريني ≈ عشرة ريالات — والمتجر نفسه يستعمل هذا التحويل */
export const BHD_TO_SAR = 10;

/** هل نصّ السعر معلَنٌ بالدينار البحريني؟ */
export const isBhdText = (text: string): boolean =>
  /BHD/i.test(text) || text.includes('د.ب');

/** يحوّل رقماً بحرينيّاً إلى ريال — والصفر وما دونه يبقى كما هو */
export const bhdToSar = (n: number): number => (n > 0 ? n * BHD_TO_SAR : n);
