/* ============ فئةٌ بلا قطعة لا تُعرض ============
 *
 * صفحات الزائر الثلاث تجلب الفئات بلا ترشيح، فأيّ فئةٍ تُنشأ تظهر فوراً:
 * قائمةٌ منسدلة في الباني، وشريحةُ ترشيح في «تصفّح القطع»، وخيارٌ في
 * المقارنة. وهذا يخلق نافذةً لا مفرّ منها: `Component.categoryId` إلزاميّ،
 * فلا سبيل لإضافة قطعةٍ قبل إنشاء فئتها — وبين الاثنين يرى الزائر قائمةً
 * فارغة ويظنّ أن الموقع معطوب.
 *
 * والحارس ليس للمبرّدات وحدها: أي فئةٍ تُفرَّغ لاحقاً (بحذف قطعها) تختفي
 * بدل أن تبقى وعداً فارغاً.
 *
 * ⚠️ ولا يُطبَّق على `/api/categories`: مسارُ الإدارة يحتاج رؤية الفئة
 * الفارغة ليضيف إليها — إخفاؤها عنه يعني أن الفئة الجديدة لا تُملأ أبداً.
 */

/** فئاتٌ فيها قطعةٌ واحدة على الأقلّ — تُمرَّر إلى `where` في Prisma */
export const HAS_PARTS = { components: { some: {} } } as const;
